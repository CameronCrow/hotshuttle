# M0 results -- measured, not computed

Generated by `experiments/m0_verify.py`. GPU at time of run: 7709 MiB used.

## VRAM + throughput per config

Decode rate is the fit test, not `memory.used`: WDDM lets the process
oversubscribe and pages the overflow to host memory, so an over-budget
config reports similar VRAM and simply runs slow.

| total ctx | KV quant | desktop baseline | with server | server cost | ingest t/s | decode t/s |
|---:|---|---:|---:|---:|---:|---:|
| 12288 | q8_0 | 356 MiB | 7701 MiB | 7345 MiB | 684 | 30.2 |

## Slot endpoints

- `GET /slots`: ok
- `POST /slots/0?action=save`: wrote 150.0 MiB
- `POST /slots/0?action=restore`: ok

## Template ground truth

Bytes the prompt compiler must reproduce exactly (`enable_thinking=false`):

```
'<|im_start|>system\nSYSTEM_MARKER<|im_end|>\n<|im_start|>user\nUSER_MARKER<|im_end|>\n<|im_start|>assistant\n<think>\n\n</think>\n\n'
```

Differs from `enable_thinking=true`: True
